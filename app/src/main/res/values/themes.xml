<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DrugBank" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="referenceOrderColor">#BB0055FF</item>
        <item name="linkColor">#BB02A6A6</item>
        <item name="infoTitleColor">@color/primary</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="snackbarTextViewStyle">@style/MaterialSnackbarTextViewTheme</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="Theme.DrugBank" parent="Base.Theme.DrugBank" />

    <!--  SnackBar  -->
    <style name="MaterialSnackbarTextViewTheme" parent="@style/Widget.Material3.Snackbar.TextView">
        <item name="android:textAlignment">textStart</item>
        <item name="android:maxLines">4</item>
    </style>
    <!--  SnackBar  -->

    <!--    Bottom sheet dialog -->
    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
    <!--    Bottom sheet dialog -->


</resources>